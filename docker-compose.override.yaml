services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
  frontend:
    build:
      context: .
      dockerfile: frontend/Dockerfile
      args:
        VITE_BACKEND_ORIGIN: ${PUBLIC_BASE_URL:-http://127.0.0.1:${BACKEND_PORT}}
